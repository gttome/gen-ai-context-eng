# One-Page Workflow Summary — v0.3.1

1. Define Iteration scope from PRD + backlog.
2. Implement complete-file changes (HTML/CSS/JS).
3. Run local smoke tests + Mobile QA scorecard.
4. Package zip with:
   - `/docs` runnable site
   - `/project-docs` updated artifacts
5. Write/update MobAI Agent Report for **current request only**.
