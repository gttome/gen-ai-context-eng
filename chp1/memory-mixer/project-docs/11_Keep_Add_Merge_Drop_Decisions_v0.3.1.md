# Keep / Add / Merge / Drop Decisions — v0.3.1

## Keep
- Mobile-first layout + static hosting compatibility
- Three memory types: rolling summary / pinned facts / retrieval memory
- Simple scoring + drift alarm
- Shared UI features (theme + version/env pills + help/feedback)

## Add (Iteration 2)
- Snapshot History modal
- Snapshot diff A→B
- Required pin hints (toggle)
- Replay panel (toggle)
- Improved drift detection
- More scenarios

## Merge
- Snapshot restore moved from prompt → modal workflow

## Drop
- Prompt-based snapshot restore UX (replaced)
