# Backlog — Memory Mixer — v0.3.1
**Request ID:** MM-ITER2-20260226  
**Date:** 2026-02-26

## Completed in Iteration 0–2
- Core memory workspace (Roll up / Pin / Retrieve)
- Drift alarm + scoring (continuity/bloat/drift risk)
- Snapshots + History + Diff
- Idle start + explicit Resume
- Retrieval search + tags + tag filter
- Conflicts detector + reconcile modal
- Export/Import Run JSON

## Next iteration candidates (Iteration 3 — optional)
### P0 (most valuable)
- Scenario Builder (add/edit turns + required pins locally)
- Stronger conflict rules for common primitives (dates, numbers, named fields)
- Export “Memory Block” styles (prompt-ready variants)

### P1
- Lightweight analytics (per-run: captures per turn, missed pins, conflict count)
- Shareable “run bundle” (zip of JSON + summary export)

### P2
- More scenarios aligned to Chapter 1 patterns (with graded hints)
