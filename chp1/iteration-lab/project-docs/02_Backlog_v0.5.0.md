# Backlog — Iteration Lab — v0.5.0

## Completed in v0.5.0 (Iteration 4 — P0)
- [x] Scenario Builder (create scenarios in-app; stored locally)
- [x] Block-level line diff view (Compare)
- [x] Export Bundle (.json): run + report + logs + templates + scenarios
- [x] Trend guardrails when test sets change (warnings + segmented line)

## Next candidates (post-P0)
### P1
- Tag tests by category + filter views
- Keyboard-first navigation (jump between blocks/tests)
- Per-block “quality checklist” hints (micro-guides)

### P2
- PWA install mode (offline-first)
- Multi-run manager (save multiple named runs and switch)
- Confidence annotations and reviewer notes per test
