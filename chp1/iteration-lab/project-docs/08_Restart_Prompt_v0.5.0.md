# Restart Prompt — Iteration Lab — v0.5.0

You are continuing my Windows-to-GitHub-Pages mobile-first HTML5 app workflow using the attached handoff package.

Follow the process docs in `project-docs/` and treat the package as source of truth.

Task: Continue Iteration Lab (Application 8) from version v0.5.0.
- Keep ZIP-FIRST and Silent Screen Output discipline.
- Generate complete files only (no partial diffs).
- Keep `start-server.bat` in the same folder as `docs/index.html`.
- Update all impacted `project-docs/*` versioned files.
- Always create/update `project-docs/28_MobAI_Agent_Report_v0.5.0.md` scoped to the current request only.
