# Keep / Add / Merge / Drop — v0.3.1

## Keep
- Static site, no build tools, vanilla JS
- Token window budgeting + overload signals
- Shared features: theme + version/env pills + help/feedback stubs
- Run history saved locally

## Add (this request)
- Tutorial overlay (auto + manual open)
- Better scoring: pass/fail gates + breakdown + anti-pattern detection
- More task cards (expanded set)
- UX polish: sorted queue by task need, requirement badges, toast feedback, cache-busting asset URLs

## Merge
- Quick fixes expanded to ensure task essentials + ordering + overload pruning

## Drop / Defer
- Physics “falling blocks” (kept as non-goal for mobile reliability)
- Drag-and-drop dependency (kept optional; tap-first baseline)
