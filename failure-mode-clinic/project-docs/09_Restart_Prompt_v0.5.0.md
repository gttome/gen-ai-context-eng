# Restart Prompt — Failure Mode Clinic v0.5.0

You are continuing the Windows-to-GitHub-Pages, mobile-first HTML5 app workflow using the attached project as source of truth.

Task: implement the next iteration for Failure Mode Clinic.

Rules:
- Generate complete files (no partial diffs).
- Zip-first, silent-screen-output when packaging is available.
- Always update `project-docs/` (PRD, backlog, architecture, test plan, security checklist, mobile QA scorecard, release notes, handoff, restart prompt, file manifest, MobAI Agent Report).
- Ensure `docs/start-server.bat` remains in the same folder as `docs/index.html`.
- Maintain static-site compatibility for GitHub Pages (relative paths; no secrets).
